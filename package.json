{
  "name": "human-utils-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "npm run hu --workspace=@human-utils/cli",
    "app:start": "npm run start --workspace=@human-utils/app",
    "app:android": "npm run android --workspace=@human-utils/app",
    "app:ios": "npm run ios --workspace=@human-utils/app",
    "app:web": "npm run web --workspace=@human-utils/app"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
